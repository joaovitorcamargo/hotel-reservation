version: '3'

services:
  api-solid-pg:
    image: bitnami/postgresql
    ports:
      - 5432:5432
    environment:
      - POSTGRESQL_USERNAME=teste
      - POSTGRESQL_PASSWORD=teste
      - POSTGRESQL_DATABASE=hotelreservation